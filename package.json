{
  "name": "ashirwad-enterprises-backend",
  "version": "1.0.0",
  "description": "Backend for Vercel deployment.",
  "main": "api/index.js",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "build": "npm run install-frontend && cd frontend && npm run build"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.2"
  }
}